# Copy this file to .env and set the endpoint that should receive contact
# form submissions as a JSON POST request. Leave it empty to show the
# "backend not configured" message on the contact form instead of faking
# a successful submission.
#
# Example: VITE_CONTACT_ENDPOINT=https://api.yourdomain.com/contact
VITE_CONTACT_ENDPOINT=
